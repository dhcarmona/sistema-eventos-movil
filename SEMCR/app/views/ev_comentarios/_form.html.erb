<%= form_for(@ev_comentario) do |f| %>
  <% if @ev_comentario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ev_comentario.errors.count, "error") %> prohibited this ev_comentario from being saved:</h2>

      <ul>
      <% @ev_comentario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.number_field :usuario_id %>
  </div>
  <div class="field">
    <%= f.label :evento_id %><br>
    <%= f.number_field :evento_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
